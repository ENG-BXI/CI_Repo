version: 2.1
jobs:
  build:
    docker:
      - image: circleci/  # استخدم إصدار محدد
      
    steps:
      - checkout
      - run:
          name: Run Flutter Doctor
          command: flutter doctor
