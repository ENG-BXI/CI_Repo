version: 2.1
jobs:
  build:
    docker:
      - image: circleci/flutter:2.5.0  # استخدم إصدار محدد
      
    steps:
      - checkout
      - run:
          name: Run Flutter Doctor
          command: flutter doctor
